import { Box, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold" gutterBottom>
        Dashboard
      </Typography>
      <Typography variant="body1">
        Welcome! Add your widgets and reports here.
      </Typography>
    </Box>
  );
};

export default Dashboard;
